import { SearchnotePipe } from './searchnote.pipe';

describe('SearchnotePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchnotePipe();
    expect(pipe).toBeTruthy();
  });
});
